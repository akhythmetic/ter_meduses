import shutil
from google.colab import files

#Chemin du fichier
folder_path = "chemin du fichier"

#Nom du fichier .zip à créer
zip_name = "frames_meduses.zip"

#Permet de crée le fichier .zip
shutil.make_archive("frames_meduses", 'zip', folder_path)

#Télécharge le .zip
files.download(zip_name)
